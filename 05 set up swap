###########swap
A lower value will make the kernel to try to avoid swapping whenever possible while a higher value means the kernel will try to use the swap space more aggressively.
check
cat /proc/sys/vm/swappiness

sudo featherpad /etc/sysctl.conf
Copy
Locate the vm.swappiness parameter and change its value. If this parameter does not exist, append the following line to the file: vm.swappiness=10
